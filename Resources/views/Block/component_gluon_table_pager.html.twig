{% block table_pager_widget %}
    {% spaceless %}
        {% set attr = attr|merge({'class': ('table-pager ' ~ attr.class|default(''))|trim, 'colspan': block.parent.vars.header.vars.header_columns|length}) %}
        <div{{ block('widget_container_attributes') }}>
            <div class="table-pager-group group1">
                <div class="btn-group">
                    <select disabled="disabled" class="table-pager-size-list btn btn-default form-control">
                        <option value="{{ source.pageSize }}">{{ source.pageSize }}</option>
                    </select>
                </div>
                <div class="btn-group">
                    <button type="button" disabled="disabled" class="table-pager-start-page btn btn-default fa fa-step-backward"></button>
                    <button type="button" disabled="disabled" class="table-pager-previous-page btn btn-default fa fa-angle-double-left"></button>
                </div>
                <div class="btn-group">
                    <input type="text" disabled="disabled" class="table-pager-page-number form-control" value="{{ source.pageNumber }}"/>
                    <span class="table-pager-split">/</span>
                    <span class="table-pager-page-count">{{ source.pageCount }}</span>
                </div>
                <div class="btn-group">
                    <button type="button" disabled="disabled" class="table-pager-next-page btn btn-default fa fa-angle-double-right"></button>
                    <button type="button" disabled="disabled" class="table-pager-end-page btn btn-default fa fa-step-forward"></button>
                </div>
            </div>
            <div class="table-pager-group group2">
                <div class="btn-group">
                    <button type="button" disabled="disabled" class="table-pager-refresh btn btn-default fa fa-refresh"></button>
                </div>
                <div class="btn-group">
                    <div class="btn btn-default disabled table-pager-elements">
                        <span class="table-pager-start">{{ source.start }}</span>
                        <span class="table-pager-split-to">-</span>
                        <span class="table-pager-end">{{ source.end }}</span>
                        <span class="table-pager-split">/</span>
                        <span class="table-pager-size">{{ source.size }}</span>
                    </div>
                </div>
                {% if block.parent.vars.attr['data-table-select'] is defined and block.parent.vars.attr['data-table-select'] == 'true' %}
                    <div class="btn-group">
                        <span class="btn btn-{{ block.parent.vars.column_selection_stype|default('secondary') }} table-select-count disabled"></span>
                        <button class="btn btn-{{ block.parent.vars.column_selection_stype|default('secondary') }} table-select-clear fa fa-times"></button>
                    </div>
                {% endif %}
            </div>
            {{- value|raw -}}
            {{- block('block_rows') -}}
        </div>
    {% endspaceless %}
{% endblock table_pager_widget %}
